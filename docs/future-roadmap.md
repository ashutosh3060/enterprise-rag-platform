# Future Roadmap — enterprise-rag-platform

## Planned

- **Query decomposition and multi-hop retrieval for questions spanning several documents** — Query decomposition and multi-hop retrieval for questions spanning several documents.
- **Incremental re-indexing on document update instead of full re-ingest** — Incremental re-indexing on document update instead of full re-ingest.
- **Automatic hard-negative mining from thumbs-down feedback to fine-tune the reranker** — Automatic hard-negative mining from thumbs-down feedback to fine-tune the reranker.

## Considered and Deferred

_Ideas explicitly rejected, with the reason. This section is often more informative
than the planned list._

## Open Questions

_Things that need data before a decision is possible._
