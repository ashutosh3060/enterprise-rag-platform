# Retrieval runs entirely offline — embeddings are local. Only answer generation
# (not yet built) needs a provider key.
ANTHROPIC_API_KEY=

# Embedding model. 384-dim, ~80MB, CPU-friendly.
RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2
